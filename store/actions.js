export default {
  toggleExpanded({ commit }) {
    commit('toggleExpanded')
  },
  toggleDarkMode({ commit }) {
    commit('toggleDarkMode')
  },
  initDarkMode({ commit }) {
    commit('initDarkMode')
  }
}
